"""Что кабинет партнёра меряет и чего не меряет (BIZ-52 срез-13, разд. 52.4).

ТЗ просит агрегировать расход по клиентам: генерации, ЭДО, хранилище, активные
сотрудники. Сверка показала, что собираются НЕ ВСЕ:

* генерации — пишутся (`tenancy_quotas` → `tenant_counters`);
* хранилище — пишется (`modules/files/api.py` → `usage_counters.s3_bytes_used`);
* активные сотрудники — пишутся задачей `billing.recompute_active_workers`;
* **ЭДО — не пишется никем**: метод инкремента есть, вызовов нет, потому что
  провайдер отправки ЭДО не реализован (остаток BIZ-50);
* **вызовы API — не пишутся вовсе**: есть колонка, нет писателя.

Показать ноль по несобираемой метрике — соврать: «клиент не пользуется ЭДО»
вместо «мы это не считаем». Поэтому такие метрики не показываются, но и не
замалчиваются — они называются в ответе списком с причиной.
"""

from __future__ import annotations

#: Метрики, которых в отчёте нет, с причиной. Текст уходит в ответ API и виден
#: человеку, поэтому он объясняет, а не отчитывается кодом.
NOT_MEASURED_METRICS: tuple[str, ...] = (
    "ЭДО: отправка не подключена, считать нечего",
    "Вызовы API: не учитываются",
)
