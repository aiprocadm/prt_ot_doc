"""Deprecated compat-shim package — canonical location is :mod:`app.modules.files` (ARCH-1).

Submodules (``s3`` / ``utils`` / ``document``) re-export from
``app.modules.files.*``; kept until POST-1 removes the layer.
"""
