"""Deprecated compat-shim package — canonical location is :mod:`app.modules.replace` (ARCH-1).

``engine`` re-exports the legacy ``ReplaceEngine`` from
``app.modules.replace.legacy_engine``; kept until POST-1 removes the layer.
"""
