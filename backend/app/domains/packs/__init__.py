"""Deprecated compat-shim package — canonical location is :mod:`app.modules.packs` (ARCH-1).

Submodules (``assets`` / ``context`` / ``definitions`` / ``seeder`` / ``service``)
re-export from ``app.modules.packs.*``; kept until POST-1 removes the layer.
"""
