"""Видимость актов НПА для соседних контуров — через назначенный слой.

ЗАЧЕМ ЭТОТ ФАЙЛ. Срез-201 завёл ЕДИНЫЙ дом правила «какие акты арендатор
вообще видит» (`app/domains/npa/scope.py`): реестровые — всем, собственные
приказы — только их владельцу. Витрина соответствия строит поисковый снимок и
обязана показывать РОВНО тот же состав: повтор условия у себя — это и есть
дефект, ради которого дом заводился (в срезе-201 такой повтор нашёлся в
сортировке списка и был убран).

Но обращаться к чужому контуру напрямую нельзя: правило ARCH-3 («не смешивать
контуры») стережёт `scripts/ci/check_context_boundaries.py`, и число исключений
в нём обязано УБЫВАТЬ, а не расти. Сторож честно поймал такой прямой импорт.

Слой `services/` назначен ТЗ как законная прослойка между контурами. Поэтому
доступ к правилу идёт через него: правило остаётся одно, а контуры не сцеплены
напрямую.
"""

from __future__ import annotations

from app.domains.npa.scope import visible_acts

__all__ = ["visible_acts"]
