# P10-04 СОУТ срез-3 — полу-авто предложения норм СИЗ/медосмотров: Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Связать СОУТ-строки с должностью (`Position`) и вредностью (`RiskHazard`) явными FK, затем выдавать read-проекцию предложений норм СИЗ и медосмотров (без записи в чужие таблицы).

**Architecture:** Два аддитивных nullable FK (`SoutWorkplace.position_id`, `SoutFactor.hazard_id`) зеркалят форму `PPENorm`. Чистый движок проекций `domains/sout/suggestions.py` (по образцу `build_report`) считает предложения из связанных строк, исключая пары, для которых норма уже существует. Медицинская проекция переиспользует движок 29н (`domains/medical/lifecycle.py`). Подтверждение — через существующий `POST /ppe/norms`; СОУТ ничего не мутирует.

**Tech Stack:** Python 3.12 (канон CI; локально `.venv\Scripts\python.exe` = 3.13.7), FastAPI, SQLAlchemy async, Alembic, pytest; фронт React/Vite + TypeScript.

**Окружение (важно):** на этой машине запускать pytest через `.venv\Scripts\python.exe` (PowerShell → file-redirect, итоги по EXIT-коду). Системный `python3` падает на `ModuleNotFoundError: boto3`. См. память `py313-win-pytest-invocation`.

---

## Файловая структура

| Файл | Ответственность | Действие |
|---|---|---|
| `backend/app/migrations/versions/20260626_so03_sout_norm_bridges.py` | Аддитивная миграция: 2 FK-колонки | Create |
| `backend/tests/test_so03_sout_norm_bridges_migration.py` | Guard: цепь single-head, форма колонок | Create |
| `backend/app/models/sout.py` | `position_id` на `SoutWorkplace`, `hazard_id` на `SoutFactor` | Modify |
| `backend/tests/test_sout_models.py` | Проверка новых полей-мостов | Modify |
| `backend/app/schemas/sout.py` | Поля-мосты + схемы предложений | Modify |
| `backend/tests/test_sout_schemas.py` | Проверка новых схем | Modify |
| `backend/app/domains/sout/suggestions.py` | Чистый движок проекций предложений | Create |
| `backend/tests/test_sout_suggestions.py` | Юнит-тесты движка (без сессии) | Create |
| `backend/app/api/routes/sout.py` | Маппинг FK в CRUD + эндпоинт предложений | Modify |
| `backend/tests/test_sout_api.py` | Роут-тесты маппинга + предложений | Modify |
| `backend/app/services/demo_bootstrap.py` | Связать демо РМ→должность, фактор→hazard | Modify |
| `frontend/src/api/sout.ts` | API-вызовы маппинга + предложений | Modify |
| `frontend/src/pages/sout/SoutPage.tsx` | Селекторы связи + секция предложений | Modify |

---

## Task 1: Миграция so03 — колонки-мосты + guard-тест

**Files:**
- Create: `backend/app/migrations/versions/20260626_so03_sout_norm_bridges.py`
- Test: `backend/tests/test_so03_sout_norm_bridges_migration.py`

- [ ] **Step 1: Написать guard-тест (падающий)**

```python
# backend/tests/test_so03_sout_norm_bridges_migration.py
"""so03 guard: single-head chain + bridge column shape (P10-04 срез-3)."""
from __future__ import annotations

from pathlib import Path

from alembic.config import Config
from alembic.script import ScriptDirectory

_REVISION = "20260626_so03_sout_norm_bridges"
_DOWN = "20260626_so02_sout_class_history"


def _script_dir() -> ScriptDirectory:
    root = Path(__file__).resolve().parents[1] / "app"
    cfg = Config()
    cfg.set_main_option("script_location", str(root / "migrations"))
    return ScriptDirectory.from_config(cfg)


def test_so03_chains_from_so02_and_is_single_head() -> None:
    sd = _script_dir()
    rev = sd.get_revision(_REVISION)
    assert rev.down_revision == _DOWN
    heads = sd.get_heads()
    assert _REVISION in heads
    assert len(heads) == 1, f"expected single head, got {heads}"


def test_so03_module_defines_both_bridge_columns() -> None:
    text = (
        Path(__file__).resolve().parents[1]
        / "app" / "migrations" / "versions" / "20260626_so03_sout_norm_bridges.py"
    ).read_text(encoding="utf-8")
    assert 'add_column("sout_workplace"' in text
    assert 'add_column("sout_factor"' in text
    assert '"position_id"' in text
    assert '"hazard_id"' in text
    assert "position.id" in text
    assert "risk_hazards.id" in text
    # honest downgrade drops both
    assert 'drop_column("sout_workplace", "position_id")' in text
    assert 'drop_column("sout_factor", "hazard_id")' in text
```

- [ ] **Step 2: Запустить — убедиться, что падает**

Run: `.venv\Scripts\python.exe -m pytest backend/tests/test_so03_sout_norm_bridges_migration.py -q`
Expected: FAIL (миграции ещё нет — `get_revision` бросает / файл не читается).

- [ ] **Step 3: Написать миграцию**

```python
# backend/app/migrations/versions/20260626_so03_sout_norm_bridges.py
"""so03: СОУТ срез-3 — bridges to Position + RiskHazard (additive, P10-04 / TZ B.10).

Two nullable FK columns mirroring ``PPENorm`` shape so СОУТ rows can resolve to the
keys норм СИЗ/медосмотров use:
  * ``sout_workplace.position_id`` -> ``position.id`` (SET NULL)
  * ``sout_factor.hazard_id``      -> ``risk_hazards.id`` (SET NULL)

SET NULL (not CASCADE): deleting a position/hazard nulls the link but never erases
the СОУТ measurement row. Honest downgrade drops both columns. Table names literal
(audit-static-analysis lesson).
"""
from __future__ import annotations

import sqlalchemy as sa
from alembic import op

revision = "20260626_so03_sout_norm_bridges"
down_revision = "20260626_so02_sout_class_history"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.add_column(
        "sout_workplace",
        sa.Column(
            "position_id",
            sa.String(length=36),
            sa.ForeignKey("position.id", ondelete="SET NULL"),
            nullable=True,
        ),
    )
    op.create_index("ix_sout_workplace_position_id", "sout_workplace", ["position_id"])
    op.add_column(
        "sout_factor",
        sa.Column(
            "hazard_id",
            sa.String(length=36),
            sa.ForeignKey("risk_hazards.id", ondelete="SET NULL"),
            nullable=True,
        ),
    )
    op.create_index("ix_sout_factor_hazard_id", "sout_factor", ["hazard_id"])


def downgrade() -> None:
    op.drop_index("ix_sout_factor_hazard_id", table_name="sout_factor")
    op.drop_column("sout_factor", "hazard_id")
    op.drop_index("ix_sout_workplace_position_id", table_name="sout_workplace")
    op.drop_column("sout_workplace", "position_id")
```

- [ ] **Step 4: Запустить — убедиться, что проходит**

Run: `.venv\Scripts\python.exe -m pytest backend/tests/test_so03_sout_norm_bridges_migration.py -q`
Expected: PASS (2 passed).

- [ ] **Step 5: Коммит**

```bash
git add backend/app/migrations/versions/20260626_so03_sout_norm_bridges.py backend/tests/test_so03_sout_norm_bridges_migration.py
git commit -m "feat(sout): so03 additive bridges to Position+RiskHazard (срез-3)"
```

---

## Task 2: ORM-поля мостов

**Files:**
- Modify: `backend/app/models/sout.py` (`SoutWorkplace` ~line 88, `SoutFactor` ~line 101)
- Test: `backend/tests/test_sout_models.py`

- [ ] **Step 1: Добавить падающий тест**

```python
# append to backend/tests/test_sout_models.py
def test_workplace_has_nullable_position_bridge() -> None:
    from app.models.sout import SoutWorkplace

    col = SoutWorkplace.__table__.c.position_id
    assert col.nullable is True
    assert any(fk.column.table.name == "position" for fk in col.foreign_keys)


def test_factor_has_nullable_hazard_bridge() -> None:
    from app.models.sout import SoutFactor

    col = SoutFactor.__table__.c.hazard_id
    assert col.nullable is True
    assert any(fk.column.table.name == "risk_hazards" for fk in col.foreign_keys)
```

- [ ] **Step 2: Запустить — убедиться, что падает**

Run: `.venv\Scripts\python.exe -m pytest backend/tests/test_sout_models.py -q -k "bridge"`
Expected: FAIL (`position_id` / `hazard_id` нет в таблице).

- [ ] **Step 3: Добавить поля в модели**

В `backend/app/models/sout.py`, в классе `SoutWorkplace` после `person_id` (строка ~87) добавить:

```python
    position_id: Mapped[str | None] = mapped_column(
        String(36), ForeignKey("position.id", ondelete="SET NULL"), nullable=True, index=True
    )
```

В классе `SoutFactor` после `workplace_id` (строка ~98) добавить:

```python
    hazard_id: Mapped[str | None] = mapped_column(
        String(36), ForeignKey("risk_hazards.id", ondelete="SET NULL"), nullable=True, index=True
    )
```

(Импорты `ForeignKey`, `String`, `Mapped`, `mapped_column` уже есть в файле.)

- [ ] **Step 4: Запустить — убедиться, что проходит**

Run: `.venv\Scripts\python.exe -m pytest backend/tests/test_sout_models.py -q`
Expected: PASS.

- [ ] **Step 5: Коммит**

```bash
git add backend/app/models/sout.py backend/tests/test_sout_models.py
git commit -m "feat(sout): position_id/hazard_id ORM bridge fields (срез-3)"
```

---

## Task 3: Схемы — поля-мосты + схемы предложений

**Files:**
- Modify: `backend/app/schemas/sout.py`
- Test: `backend/tests/test_sout_schemas.py`

- [ ] **Step 1: Добавить падающий тест**

```python
# append to backend/tests/test_sout_schemas.py
from app.models.sout import SoutClass
from app.schemas.sout import (
    FactorUpdate,
    MedicalExamSuggestion,
    NormSuggestions,
    PpeNormSuggestion,
    WorkplaceCreate,
)


def test_workplace_create_accepts_position_id() -> None:
    wp = WorkplaceCreate(workplace_code="РМ-1", position_name="Сварщик", position_id="pos-1")
    assert wp.position_id == "pos-1"


def test_factor_update_accepts_hazard_id() -> None:
    fu = FactorUpdate(hazard_id="haz-1")
    assert fu.model_dump(exclude_unset=True) == {"hazard_id": "haz-1"}


def test_norm_suggestions_envelope() -> None:
    ppe = PpeNormSuggestion(
        position_id="pos-1", hazard_id="haz-1", hazard_title="Шум",
        factor_name="Шум", factor_code="4.50", measured_class=SoutClass.HARMFUL_3_1,
        reason="demo",
    )
    med = MedicalExamSuggestion(
        position_id="pos-1", exam_kind="periodic", periodicity_months=12,
        factor_codes=["4.4"], reason="demo",
    )
    env = NormSuggestions(ppe=[ppe], medical=[med])
    assert env.ppe[0].hazard_id == "haz-1"
    assert env.medical[0].exam_kind == "periodic"
```

- [ ] **Step 2: Запустить — убедиться, что падает**

Run: `.venv\Scripts\python.exe -m pytest backend/tests/test_sout_schemas.py -q -k "position_id or hazard_id or suggestion or envelope"`
Expected: FAIL (ImportError — новых схем нет).

- [ ] **Step 3: Расширить схемы**

В `backend/app/schemas/sout.py`:

В `WorkplaceCreate` добавить поле:
```python
    position_id: str | None = None
```
В `WorkplaceUpdate` добавить:
```python
    position_id: str | None = None
```
В `WorkplaceRead` добавить (после `person_id`):
```python
    position_id: str | None
```
В `FactorCreate` добавить:
```python
    hazard_id: str | None = None
```
В `FactorRead` добавить (после `workplace_id`):
```python
    hazard_id: str | None
```
Добавить новую схему `FactorUpdate` (её ещё нет):
```python
class FactorUpdate(BaseSchema):
    name: str | None = None
    code: str | None = None
    measured_class: SoutClass | None = None
    note: str | None = None
    hazard_id: str | None = None
```
В конец файла добавить схемы предложений:
```python
# --- Norm suggestions (срез-3) ---
class PpeNormSuggestion(BaseSchema):
    position_id: str
    hazard_id: str
    hazard_title: str
    factor_name: str
    factor_code: str | None
    measured_class: SoutClass | None
    reason: str


class MedicalExamSuggestion(BaseSchema):
    position_id: str
    exam_kind: str
    periodicity_months: int
    factor_codes: list[str]
    reason: str


class NormSuggestions(BaseSchema):
    ppe: list[PpeNormSuggestion]
    medical: list[MedicalExamSuggestion]
```

- [ ] **Step 4: Запустить — убедиться, что проходит**

Run: `.venv\Scripts\python.exe -m pytest backend/tests/test_sout_schemas.py -q`
Expected: PASS.

- [ ] **Step 5: Коммит**

```bash
git add backend/app/schemas/sout.py backend/tests/test_sout_schemas.py
git commit -m "feat(sout): bridge fields + norm-suggestion schemas (срез-3)"
```

---

## Task 4: Движок предложений СИЗ (чистая функция)

**Files:**
- Create: `backend/app/domains/sout/suggestions.py`
- Test: `backend/tests/test_sout_suggestions.py`

- [ ] **Step 1: Написать падающие тесты**

```python
# backend/tests/test_sout_suggestions.py
"""Pure-function unit tests for СОУТ norm-suggestion engine (срез-3)."""
from __future__ import annotations

from types import SimpleNamespace

from app.domains.sout.suggestions import build_ppe_norm_suggestions
from app.models.sout import SoutClass


def _factor(hazard_id, name="Шум", code="4.50", cls=SoutClass.HARMFUL_3_1):
    return SimpleNamespace(hazard_id=hazard_id, name=name, code=code, measured_class=cls)


def test_ppe_suggestion_for_linked_pair_without_norm() -> None:
    out = build_ppe_norm_suggestions(
        position_id="pos-1",
        factors=[_factor("haz-1")],
        hazard_titles={"haz-1": "Шум"},
        existing_norm_pairs=set(),
    )
    assert len(out) == 1
    s = out[0]
    assert (s.position_id, s.hazard_id) == ("pos-1", "haz-1")
    assert s.hazard_title == "Шум"
    assert "Шум" in s.reason


def test_ppe_dedup_when_norm_exists_for_pair() -> None:
    out = build_ppe_norm_suggestions(
        position_id="pos-1",
        factors=[_factor("haz-1")],
        hazard_titles={"haz-1": "Шум"},
        existing_norm_pairs={("pos-1", "haz-1")},
    )
    assert out == []


def test_ppe_empty_when_workplace_unlinked() -> None:
    out = build_ppe_norm_suggestions(
        position_id=None,
        factors=[_factor("haz-1")],
        hazard_titles={"haz-1": "Шум"},
        existing_norm_pairs=set(),
    )
    assert out == []


def test_ppe_skips_factor_without_hazard_link() -> None:
    out = build_ppe_norm_suggestions(
        position_id="pos-1",
        factors=[_factor(None)],
        hazard_titles={},
        existing_norm_pairs=set(),
    )
    assert out == []


def test_ppe_collapses_two_factors_on_same_hazard() -> None:
    out = build_ppe_norm_suggestions(
        position_id="pos-1",
        factors=[_factor("haz-1", name="Шум"), _factor("haz-1", name="Вибрация")],
        hazard_titles={"haz-1": "Физический"},
        existing_norm_pairs=set(),
    )
    assert len(out) == 1
```

- [ ] **Step 2: Запустить — убедиться, что падает**

Run: `.venv\Scripts\python.exe -m pytest backend/tests/test_sout_suggestions.py -q`
Expected: FAIL (модуля `suggestions` нет).

- [ ] **Step 3: Написать движок (часть СИЗ)**

```python
# backend/app/domains/sout/suggestions.py
"""Pure read-projection engine for СОУТ norm suggestions (срез-3, P10-04).

No DB access — callers pass already-loaded rows (or SimpleNamespace in tests).
СИЗ side dedups by ``(position_id, hazard_id)`` presence in existing PPENorm;
medical side reuses the 29н engine and dedups by ``(position_id, exam_kind)``.
Nothing here mutates target tables — confirmation goes through the targets' own
write endpoints (e.g. ``POST /ppe/norms``).
"""
from __future__ import annotations

from typing import Iterable

from app.domains.medical import lifecycle as med_lc
from app.schemas.sout import MedicalExamSuggestion, PpeNormSuggestion


def build_ppe_norm_suggestions(
    *,
    position_id: str | None,
    factors: Iterable,
    hazard_titles: dict[str, str],
    existing_norm_pairs: set[tuple[str, str]],
) -> list[PpeNormSuggestion]:
    """Propose a PPE norm per linked (position, hazard) pair lacking any norm.

    ``factors`` are SoutFactor rows (each exposes ``hazard_id``/``name``/``code``/
    ``measured_class``). A pair already covered by an existing PPENorm (present in
    ``existing_norm_pairs``) is skipped — item_name is not guessed, so the signal is
    simply "this harmful factor has no PPE norm yet". Multiple factors on the same
    hazard collapse to one suggestion.
    """
    if position_id is None:
        return []
    out: list[PpeNormSuggestion] = []
    seen: set[tuple[str, str]] = set()
    for f in factors:
        hid = getattr(f, "hazard_id", None)
        if hid is None:
            continue
        pair = (position_id, hid)
        if pair in existing_norm_pairs or pair in seen:
            continue
        seen.add(pair)
        cls = getattr(f, "measured_class", None)
        cls_label = cls.value if cls is not None else "—"
        out.append(
            PpeNormSuggestion(
                position_id=position_id,
                hazard_id=hid,
                hazard_title=hazard_titles.get(hid, ""),
                factor_name=getattr(f, "name", ""),
                factor_code=getattr(f, "code", None),
                measured_class=cls,
                reason=f"СОУТ: вредный фактор «{getattr(f, 'name', '')}» (класс {cls_label}) "
                f"без нормы СИЗ для должности",
            )
        )
    return out
```

- [ ] **Step 4: Запустить — убедиться, что проходит**

Run: `.venv\Scripts\python.exe -m pytest backend/tests/test_sout_suggestions.py -q`
Expected: PASS (5 passed).

- [ ] **Step 5: Коммит**

```bash
git add backend/app/domains/sout/suggestions.py backend/tests/test_sout_suggestions.py
git commit -m "feat(sout): PPE norm-suggestion projection engine (срез-3)"
```

---

## Task 5: Движок предложений медосмотров (переиспользование 29н)

**Files:**
- Modify: `backend/app/domains/sout/suggestions.py`
- Test: `backend/tests/test_sout_suggestions.py`

Контекст сигнатур (verbatim, `backend/app/domains/medical/lifecycle.py`):
- `factors_for_hazards(hazard_factor_codes: set[str], catalog: Iterable[FactorTuple]) -> set[FactorTuple]`
- `required_exams_from_factors(factors: Iterable[FactorTuple]) -> dict[MedicalExamKind, int]`
- `FactorTuple = tuple[code: str, name: str, exam_kinds: tuple[MedicalExamKind, ...], periodicity_months: int]`

- [ ] **Step 1: Добавить падающие тесты**

```python
# append to backend/tests/test_sout_suggestions.py
from app.domains.sout.suggestions import build_medical_exam_suggestions
from app.models.models import MedicalExamKind


def _catalog():
    # FactorTuple: (code, name, exam_kinds_tuple, periodicity_months)
    return [
        ("4.4", "Шум", (MedicalExamKind.PERIODIC, MedicalExamKind.FLUOROGRAPHY), 12),
    ]


def test_medical_suggestion_from_linked_hazard_factor() -> None:
    out = build_medical_exam_suggestions(
        position_id="pos-1",
        hazard_factor_codes={"4.4"},
        factor_catalog=_catalog(),
        existing_norm_kinds=set(),
    )
    kinds = {s.exam_kind for s in out}
    assert "periodic" in kinds
    assert "fluorography" in kinds
    periodic = next(s for s in out if s.exam_kind == "periodic")
    assert periodic.periodicity_months == 12
    assert "4.4" in periodic.factor_codes


def test_medical_dedup_existing_norm_kind() -> None:
    out = build_medical_exam_suggestions(
        position_id="pos-1",
        hazard_factor_codes={"4.4"},
        factor_catalog=_catalog(),
        existing_norm_kinds={MedicalExamKind.PERIODIC},
    )
    kinds = {s.exam_kind for s in out}
    assert "periodic" not in kinds
    assert "fluorography" in kinds


def test_medical_empty_when_unlinked_position() -> None:
    out = build_medical_exam_suggestions(
        position_id=None,
        hazard_factor_codes={"4.4"},
        factor_catalog=_catalog(),
        existing_norm_kinds=set(),
    )
    assert out == []
```

- [ ] **Step 2: Запустить — убедиться, что падает**

Run: `.venv\Scripts\python.exe -m pytest backend/tests/test_sout_suggestions.py -q -k "medical"`
Expected: FAIL (`build_medical_exam_suggestions` нет).

- [ ] **Step 3: Дополнить движок (медицинская часть)**

В `backend/app/domains/sout/suggestions.py` добавить функцию:

```python
def build_medical_exam_suggestions(
    *,
    position_id: str | None,
    hazard_factor_codes: set[str],
    factor_catalog: Iterable,
    existing_norm_kinds: set,
) -> list[MedicalExamSuggestion]:
    """Propose required medical exams derived from linked hazards' 29н factors.

    Reuses the medical engine: linked hazards carry ``medical_factor_code`` →
    ``factors_for_hazards`` filters the catalog → ``required_exams_from_factors``
    yields kind→strictest-periodicity. A kind already covered by an existing
    MedicalNorm for the position (``existing_norm_kinds``) is skipped.
    """
    if position_id is None:
        return []
    matched = med_lc.factors_for_hazards(set(hazard_factor_codes), factor_catalog)
    required = med_lc.required_exams_from_factors(matched)  # dict[MedicalExamKind, int]
    out: list[MedicalExamSuggestion] = []
    for kind, months in required.items():
        if kind in existing_norm_kinds:
            continue
        codes = sorted(
            code for (code, _name, kinds, _m) in matched if kind in kinds
        )
        out.append(
            MedicalExamSuggestion(
                position_id=position_id,
                exam_kind=kind.value,
                periodicity_months=int(months),
                factor_codes=codes,
                reason=f"СОУТ: факторы 29н {codes} требуют осмотр «{kind.value}» "
                f"каждые {int(months)} мес. — нормы нет",
            )
        )
    return out
```

- [ ] **Step 4: Запустить — убедиться, что проходит**

Run: `.venv\Scripts\python.exe -m pytest backend/tests/test_sout_suggestions.py -q`
Expected: PASS (8 passed).

- [ ] **Step 5: Коммит**

```bash
git add backend/app/domains/sout/suggestions.py backend/tests/test_sout_suggestions.py
git commit -m "feat(sout): medical exam-suggestion projection reusing 29н engine (срез-3)"
```

---

## Task 6: Роут — маппинг FK (должность + вредность)

**Files:**
- Modify: `backend/app/api/routes/sout.py`
- Test: `backend/tests/test_sout_api.py`

Существующие тесты роута патчат `_get_workplace`/`_get_campaign` (см. комментарий «patched in tests» в роуте). Новые валидирующие геттеры должны быть в том же стиле, чтобы тесты могли их подменять.

- [ ] **Step 1: Написать падающие тесты**

```python
# append to backend/tests/test_sout_api.py
# (follow the existing test file's client/tenant/monkeypatch fixtures style)

import app.api.routes.sout as sout_routes


def test_patch_workplace_sets_position_id(client, tenant, monkeypatch):
    wp = _make_workplace(tenant)  # existing helper in this test module
    monkeypatch.setattr(sout_routes, "_get_workplace", _const(wp))
    monkeypatch.setattr(sout_routes, "_validate_position", _const("pos-1"))
    resp = client.patch(f"/sout/workplaces/{wp.id}", json={"position_id": "pos-1"})
    assert resp.status_code == 200
    assert resp.json()["position_id"] == "pos-1"


def test_patch_workplace_bad_position_404(client, tenant, monkeypatch):
    wp = _make_workplace(tenant)
    monkeypatch.setattr(sout_routes, "_get_workplace", _const(wp))

    async def _boom(session, t, pid):
        raise sout_routes._not_found("Position")

    monkeypatch.setattr(sout_routes, "_validate_position", _boom)
    resp = client.patch(f"/sout/workplaces/{wp.id}", json={"position_id": "nope"})
    assert resp.status_code == 404


def test_patch_factor_sets_hazard_id(client, tenant, monkeypatch):
    fac = _make_factor(tenant)  # existing/added helper
    monkeypatch.setattr(sout_routes, "_get_factor", _const(fac))
    monkeypatch.setattr(sout_routes, "_validate_hazard", _const("haz-1"))
    resp = client.patch(f"/sout/factors/{fac.id}", json={"hazard_id": "haz-1"})
    assert resp.status_code == 200
    assert resp.json()["hazard_id"] == "haz-1"
```

> Если в `test_sout_api.py` нет хелперов `_make_workplace`/`_make_factor`/`_const`/`_make_*`, добавить минимальные по образцу существующих тестов файла (SimpleNamespace-фабрики + async-обёртка `_const` возвращающая корутину). Сверить фактический стиль файла перед написанием.

- [ ] **Step 2: Запустить — убедиться, что падает**

Run: `.venv\Scripts\python.exe -m pytest backend/tests/test_sout_api.py -q -k "position_id or hazard_id or factor"`
Expected: FAIL (нет `_validate_position`/`_get_factor`/PATCH factor).

- [ ] **Step 3: Добавить геттеры/валидаторы и маппинг в роут**

В `backend/app/api/routes/sout.py`:

Импорты — добавить модели целей и схему `FactorUpdate`/`NormSuggestions`:
```python
from app.models.models import MedicalNorm, PPENorm, Position
from app.models.risk import RiskHazard
```
и в блок импорта схем добавить `FactorUpdate`.

После `_get_workplace` добавить валидаторы и геттер фактора:
```python
async def _get_factor(session: AsyncSession, tenant: Tenant, fid: str) -> SoutFactor:
    row = (
        await session.execute(
            select(SoutFactor).where(
                SoutFactor.id == fid, SoutFactor.tenant_id == tenant.id
            )
        )
    ).scalar_one_or_none()
    if row is None:
        raise _not_found("Factor")
    return row


async def _validate_position(session: AsyncSession, tenant: Tenant, pid: str) -> str:
    row = (
        await session.execute(
            select(Position.id).where(
                Position.id == pid,
                Position.tenant_id == tenant.id,
                Position.deleted_at.is_(None),
            )
        )
    ).scalar_one_or_none()
    if row is None:
        raise _not_found("Position")
    return pid


async def _validate_hazard(session: AsyncSession, tenant: Tenant, hid: str) -> str:
    # RiskHazard has no soft-delete column — scope by tenant only.
    row = (
        await session.execute(
            select(RiskHazard.id).where(
                RiskHazard.id == hid, RiskHazard.tenant_id == tenant.id
            )
        )
    ).scalar_one_or_none()
    if row is None:
        raise _not_found("Hazard")
    return hid
```

В `add_workplace` (создание РМ) пробросить `position_id` с валидацией — после `ensure_campaign_open`, перед созданием строки:
```python
    if payload.position_id is not None:
        await _validate_position(session, tenant, payload.position_id)
```
и в конструктор `SoutWorkplace(...)` добавить `position_id=payload.position_id,`.

В `update_workplace` — валидировать перед применением, если в changes есть непустой `position_id`:
```python
    changes = payload.model_dump(exclude_unset=True)
    if changes.get("position_id") is not None:
        await _validate_position(session, tenant, changes["position_id"])
```
(оставить существующую логику истории класса без изменений.)

В `add_factor` (создание фактора) — добавить валидацию и поле:
```python
    if payload.hazard_id is not None:
        await _validate_hazard(session, tenant, payload.hazard_id)
```
и в конструктор `SoutFactor(...)` добавить `hazard_id=payload.hazard_id,`.

Добавить новый эндпоинт PATCH фактора (его сейчас нет) после `add_factor`:
```python
@router.patch("/factors/{fid}", response_model=FactorRead)
async def update_factor(
    fid: str, payload: FactorUpdate, tenant: TenantDep, session: SessionDep, access: Access
) -> FactorRead:
    TenantContextValidator.ensure_tenant_context(tenant)
    await _require_sout_enabled(session, tenant)
    row = await _get_factor(session, tenant, fid)
    changes = payload.model_dump(exclude_unset=True)
    if changes.get("hazard_id") is not None:
        await _validate_hazard(session, tenant, changes["hazard_id"])
    for field, value in changes.items():
        setattr(row, field, value)
    await session.flush()
    await session.refresh(row)
    return FactorRead.model_validate(row, from_attributes=True)
```

- [ ] **Step 4: Запустить — убедиться, что проходит**

Run: `.venv\Scripts\python.exe -m pytest backend/tests/test_sout_api.py -q`
Expected: PASS.

- [ ] **Step 5: Коммит**

```bash
git add backend/app/api/routes/sout.py backend/tests/test_sout_api.py
git commit -m "feat(sout): manual position/hazard mapping in workplace+factor CRUD (срез-3)"
```

---

## Task 7: Роут — `GET /sout/workplaces/{wid}/norm-suggestions`

**Files:**
- Modify: `backend/app/api/routes/sout.py`
- Test: `backend/tests/test_sout_api.py`

Контекст (verbatim, для загрузки данных):
- Каталог 29н: `from app.domains.medical.service import _load_factor_catalog` → `await _load_factor_catalog(session, tenant_id) -> list[FactorTuple]`.
- `PPENorm(position_id, hazard_id, ...)`; `MedicalNorm(position_id, exam_kind, ...)` (`exam_kind` = native enum `MedicalExamKind`).

- [ ] **Step 1: Написать падающий тест**

```python
# append to backend/tests/test_sout_api.py
def test_norm_suggestions_shape(client, tenant, monkeypatch):
    wp = _make_workplace(tenant, position_id="pos-1")
    fac = _make_factor(tenant, hazard_id="haz-1")
    monkeypatch.setattr(sout_routes, "_get_workplace", _const(wp))

    async def _factors(session, t, wid):
        return [fac]

    async def _hazards(session, t, ids):
        return {"haz-1": ("Шум", "4.4")}

    async def _ppe_pairs(session, t, pid):
        return set()

    async def _med(session, t, pid):
        return ([("4.4", "Шум", (), 12)], set())

    monkeypatch.setattr(sout_routes, "_load_workplace_factors", _factors)
    monkeypatch.setattr(sout_routes, "_load_hazard_meta", _hazards)
    monkeypatch.setattr(sout_routes, "_load_existing_ppe_pairs", _ppe_pairs)
    monkeypatch.setattr(sout_routes, "_load_medical_inputs", _med)

    resp = client.get(f"/sout/workplaces/{wp.id}/norm-suggestions")
    assert resp.status_code == 200
    body = resp.json()
    assert "ppe" in body and "medical" in body


def test_norm_suggestions_empty_for_unlinked_workplace(client, tenant, monkeypatch):
    wp = _make_workplace(tenant, position_id=None)
    monkeypatch.setattr(sout_routes, "_get_workplace", _const(wp))
    resp = client.get(f"/sout/workplaces/{wp.id}/norm-suggestions")
    assert resp.status_code == 200
    assert resp.json() == {"ppe": [], "medical": []}
```

> Сверить фактические fixture-имена в `test_sout_api.py`; при необходимости расширить `_make_workplace`/`_make_factor`, чтобы принимать `position_id=`/`hazard_id=`.

- [ ] **Step 2: Запустить — убедиться, что падает**

Run: `.venv\Scripts\python.exe -m pytest backend/tests/test_sout_api.py -q -k "norm_suggestions"`
Expected: FAIL (эндпоинта нет).

- [ ] **Step 3: Реализовать загрузчики + эндпоинт**

В `backend/app/api/routes/sout.py` добавить импорт движка и схемы:
```python
from app.domains.sout.suggestions import (
    build_medical_exam_suggestions,
    build_ppe_norm_suggestions,
)
from app.domains.medical.service import _load_factor_catalog
from app.schemas.sout import NormSuggestions
```

Добавить тонкие загрузчики (выделены отдельно, чтобы тесты подменяли их):
```python
async def _load_workplace_factors(session, tenant, wid):
    return list(
        (
            await session.execute(
                select(SoutFactor).where(
                    SoutFactor.workplace_id == wid, SoutFactor.tenant_id == tenant.id
                )
            )
        ).scalars().all()
    )


async def _load_hazard_meta(session, tenant, hazard_ids):
    """Return {hazard_id: (title, medical_factor_code)} for the given ids."""
    if not hazard_ids:
        return {}
    rows = (
        await session.execute(
            select(RiskHazard.id, RiskHazard.title, RiskHazard.medical_factor_code).where(
                RiskHazard.id.in_(hazard_ids), RiskHazard.tenant_id == tenant.id
            )
        )
    ).all()
    return {r[0]: (r[1], r[2]) for r in rows}


async def _load_existing_ppe_pairs(session, tenant, position_id):
    rows = (
        await session.execute(
            select(PPENorm.position_id, PPENorm.hazard_id).where(
                PPENorm.tenant_id == tenant.id, PPENorm.position_id == position_id
            )
        )
    ).all()
    return {(r[0], r[1]) for r in rows}


async def _load_medical_inputs(session, tenant, position_id):
    """Return (factor_catalog, existing_norm_kinds) for the position."""
    catalog = await _load_factor_catalog(session, str(tenant.id))
    kinds = set(
        (
            await session.execute(
                select(MedicalNorm.exam_kind).where(
                    MedicalNorm.tenant_id == tenant.id,
                    MedicalNorm.position_id == position_id,
                )
            )
        ).scalars().all()
    )
    return catalog, kinds
```

Добавить эндпоинт после `list_class_history`:
```python
@router.get("/workplaces/{wid}/norm-suggestions", response_model=NormSuggestions)
async def get_norm_suggestions(
    wid: str, tenant: TenantDep, session: SessionDep, access: Access
) -> NormSuggestions:
    TenantContextValidator.ensure_tenant_context(tenant)
    await _require_sout_enabled(session, tenant)
    wp = await _get_workplace(session, tenant, wid)
    if wp.position_id is None:
        return NormSuggestions(ppe=[], medical=[])
    factors = await _load_workplace_factors(session, tenant, wid)
    hazard_ids = {f.hazard_id for f in factors if f.hazard_id is not None}
    hazard_meta = await _load_hazard_meta(session, tenant, hazard_ids)
    hazard_titles = {hid: meta[0] for hid, meta in hazard_meta.items()}
    ppe_pairs = await _load_existing_ppe_pairs(session, tenant, wp.position_id)
    ppe = build_ppe_norm_suggestions(
        position_id=wp.position_id,
        factors=factors,
        hazard_titles=hazard_titles,
        existing_norm_pairs=ppe_pairs,
    )
    factor_codes = {meta[1] for meta in hazard_meta.values() if meta[1]}
    catalog, existing_kinds = await _load_medical_inputs(session, tenant, wp.position_id)
    medical = build_medical_exam_suggestions(
        position_id=wp.position_id,
        hazard_factor_codes=factor_codes,
        factor_catalog=catalog,
        existing_norm_kinds=existing_kinds,
    )
    return NormSuggestions(ppe=ppe, medical=medical)
```

- [ ] **Step 4: Запустить — убедиться, что проходит**

Run: `.venv\Scripts\python.exe -m pytest backend/tests/test_sout_api.py -q`
Expected: PASS.

- [ ] **Step 5: Коммит**

```bash
git add backend/app/api/routes/sout.py backend/tests/test_sout_api.py
git commit -m "feat(sout): GET norm-suggestions read-projection endpoint (срез-3)"
```

---

## Task 8: Фронтенд (тонкий) — связь + секция предложений

**Files:**
- Modify: `frontend/src/api/sout.ts`
- Modify: `frontend/src/pages/sout/SoutPage.tsx`

Цель — минимальная поверхность: связать должность/вредность и показать предложения. Сверить точные типы/паттерны существующего `sout.ts` (там уже есть `getWorkplaceClassHistory` из среза-2 — зеркалить его форму).

- [ ] **Step 1: Расширить API-клиент**

В `frontend/src/api/sout.ts` добавить типы и функции (зеркаля существующие):
```typescript
export interface PpeNormSuggestion {
  position_id: string;
  hazard_id: string;
  hazard_title: string;
  factor_name: string;
  factor_code: string | null;
  measured_class: string | null;
  reason: string;
}

export interface MedicalExamSuggestion {
  position_id: string;
  exam_kind: string;
  periodicity_months: number;
  factor_codes: string[];
  reason: string;
}

export interface NormSuggestions {
  ppe: PpeNormSuggestion[];
  medical: MedicalExamSuggestion[];
}

export async function getNormSuggestions(workplaceId: string): Promise<NormSuggestions> {
  const { data } = await api.get<NormSuggestions>(`/sout/workplaces/${workplaceId}/norm-suggestions`);
  return data;
}

export async function linkWorkplacePosition(workplaceId: string, positionId: string): Promise<void> {
  await api.patch(`/sout/workplaces/${workplaceId}`, { position_id: positionId });
}

export async function linkFactorHazard(factorId: string, hazardId: string): Promise<void> {
  await api.patch(`/sout/factors/${factorId}`, { hazard_id: hazardId });
}
```
(использовать тот же `api`/axios-инстанс и стиль, что уже в файле.)

- [ ] **Step 2: Добавить lazy-секцию предложений в карточку РМ**

В `frontend/src/pages/sout/SoutPage.tsx` — по образцу lazy-секции «История изменения класса» (срез-2) добавить раздел «Предложения норм»: кнопка/раскрытие → `getNormSuggestions(workplaceId)` → список `ppe[]` (вредность + причина) и `medical[]` (вид осмотра + периодичность) с пометкой «подтвердите в разделе СИЗ / Медосмотры». Только чтение — без inline-создания норм. Селекторы связи должности/вредности — простые текстовые поля id + кнопка (минимально), вызывающие `linkWorkplacePosition`/`linkFactorHazard`.

- [ ] **Step 3: Проверить типизацию**

Run: `cd frontend && npm run typecheck` (или существующая команда проверки типов проекта)
Expected: exit 0.

- [ ] **Step 4: Коммит**

```bash
git add frontend/src/api/sout.ts frontend/src/pages/sout/SoutPage.tsx
git commit -m "feat(sout): thin frontend — link position/hazard + norm suggestions (срез-3)"
```

---

## Task 9: Demo-seed + регрессия + handoff

**Files:**
- Modify: `backend/app/services/demo_bootstrap.py` (`_seed_sout_demo` ~line 693 + call site)

- [ ] **Step 1: Связать демо РМ→должность и фактор→hazard**

Изменить сигнатуру `_seed_sout_demo(session, tenant_db_id, position_id)` и пробросить `position_id` из вызывающего `bootstrap_demo_tenant` (там, где другие seed'ы уже получают `position_id`). В теле, ПЕРЕД idempotent-bail (или при создании, чтобы не ломать идемпотентность), установить:
- `workplace.position_id = position_id`;
- найти демо-`RiskHazard` с `code == "demo_general"` (как делают `_seed_ppe_demo`/`_seed_medical_factor_demo`) и установить `factor.hazard_id = hazard.id`.

Код для тела (создающая ветка, после `await session.flush()` на campaign):
```python
    from app.models.risk import RiskHazard

    demo_hazard = (
        await session.execute(
            select(RiskHazard).where(
                RiskHazard.tenant_id == tenant_db_id,
                RiskHazard.code == "demo_general",
            )
        )
    ).scalar_one_or_none()

    workplace = SoutWorkplace(
        ...,                      # existing fields unchanged
        position_id=position_id,  # NEW: link to demo position
    )
    ...
    session.add(
        SoutFactor(
            ...,                                   # existing fields unchanged
            hazard_id=demo_hazard.id if demo_hazard else None,  # NEW: link to demo hazard
        )
    )
```
(Замечание: демо-`demo_general` уже имеет `PPENorm` и `medical_factor_code="4.4"` из соседних seed'ов — поэтому демо-предложения СИЗ/медосмотров корректно окажутся ПУСТЫМИ через дедуп. Это валидная демонстрация поведения; первичное доказательство корректности — тесты. Менять норм-seed'ы не нужно.)

- [ ] **Step 2: Прогон контурного когорта**

Run: `.venv\Scripts\python.exe -m pytest backend/tests/ -q -k "sout or so01 or so02 or so03"`
Expected: PASS (срез-1/2/3 целиком зелёные; ~70+ тестов).

- [ ] **Step 3: Смежная регрессия (миграции/мапперы + затронутые движки)**

Run: `.venv\Scripts\python.exe -m pytest backend/tests/ -q -k "migration or downgrade or mapper or medical or ppe"`
Expected: PASS (мост к `Position`/`RiskHazard`/`PPENorm`/`MedicalNorm` ничего не ломает; переиспользование 29н-движка не меняет его поведение).

- [ ] **Step 4: Коммит**

```bash
git add backend/app/services/demo_bootstrap.py
git commit -m "feat(sout): demo-seed links workplace->position, factor->hazard (срез-3)"
```

- [ ] **Step 5: Обновить handoff/память**

Обновить `AI_IMPLEMENTATION_REPORT.md` (Last Agent Handoff для P10-04 срез-3) и память `sout-p10-04-srez1.md`: срез-3 BUILT, ветка `feat/sout-srez3-p10-04`, объём, тесты, что отложено (авто-запись норм, декларация, импорт файла, печатные формы). Закоммитить.

---

## Self-Review (выполнено при написании плана)

**Spec coverage:**
- Мост `position_id`/`hazard_id` → Task 1–2. ✓
- Read-проекция, дедуп → Task 4–5, 7. ✓
- СИЗ + медосмотры отдельно → Task 4 (СИЗ) + Task 5 (медосмотры через 29н). ✓
- Ручной маппинг через CRUD → Task 6. ✓
- Эндпоинт предложений → Task 7. ✓
- Тонкий фронт → Task 8. ✓
- Demo-seed + тесты → Task 9. ✓
- Обработка ошибок (404 на битый FK, пусто для несвязанного РМ) → Task 6–7. ✓

**Type consistency:** `build_ppe_norm_suggestions`/`build_medical_exam_suggestions` сигнатуры идентичны в Task 4/5 (определение) и Task 7 (вызов). Схемы `PpeNormSuggestion`/`MedicalExamSuggestion`/`NormSuggestions` — Task 3 (определение) → Task 4/5/7 (использование). `_validate_position`/`_validate_hazard`/`_get_factor` — Task 6 (определение) → Task 7 (соседний код). ✓

**Placeholder scan:** код приведён в каждом шаге; фронт-Task 8 Step 2 описателен по необходимости (зеркалит существующую lazy-секцию среза-2, точные JSX-детали — по факту файла). Помечено «сверить фактический стиль».

**Известные точки сверки при реализации (не плейсхолдеры, а честные зависимости от факта):**
- Стиль фикстур в `test_sout_api.py` (хелперы `_make_workplace`/`_const`) — сверить и переиспользовать.
- Команда typecheck фронта — сверить `frontend/package.json`.
- Точка вызова `_seed_sout_demo` в `bootstrap_demo_tenant` (проброс `position_id`).
