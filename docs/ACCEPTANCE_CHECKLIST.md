# ACCEPTANCE CHECKLIST (RC)

Статусы: ✅ выполнено · 🟡 частично · ⛔ заблокировано.

1. **Повторный POST с тем же `Idempotency-Key`** — ✅  
   Подтверждено `tests/test_idempotency.py`.

2. **Любой бизнес-маршрут без `X-Tenant` -> 400** — 🟡  
   Подтверждено для целевых бизнес-роутов tenancy; webhook ingress оставлен как осознанное исключение (публичный callback-контур).

3. **Выбор шаблона по (коду, версии), конфликтное удаление -> корректная ошибка** — 🟡  
   В текущем проходе не проводился полный контрактный прогон шаблонов.

4. **Replace: dry-run -> apply -> rollback** — ⛔  
   Требует отдельного стабилизационного цикла replace API.

5. **DocumentJob статусы queue/running/success/error/cancelled** — ✅  
   Подтверждено `tests/integration/test_job_status_flow.py`.

6. **Кабинет клиента: свои статусы/пакеты/файлы/история** — 🟡  
   Требуется отдельный интеграционный e2e проход (не закрыт в этом финальном хотфиксе).

7. **Пути дашборд/отчёты/экспорт работают** — 🟡  
   Не прогонялись end-to-end в данном узком стабилизационном цикле.

8. **Risk/PPE/Training/Incidents/Inspections CRUD не ломают поток** — 🟡  
   Не покрыто полным регрессионным набором в этой итерации.

9. **Frontend/Backend не расходятся по ключевым разделам** — 🟡  
   Frontend build проходит; контрактный full-scan не завершён полностью.

10. **CI без критических регрессий** — 🟡  
   Критичный регресс по webhook tenant-context закрыт; полный CI-маршрут требует дополнительного запуска всех пайплайнов.
