# ACCEPTANCE CHECKLIST (RC)

Статусы: ✅ выполнено · 🟡 частично · ⛔ заблокировано.

1. **Повторный POST с тем же `Idempotency-Key`** — 🟡  
   Подтверждено на паковом запуске (целевые тесты проходят), но есть несвязанные с идемпотентностью красные API-блоки в полном прогоне.

2. **Любой бизнес-маршрут без `X-Tenant` -> 400** — 🟡  
   Критичные сценарии tenancy + webhook ingress проверены и стабилизированы; остаются отдельные исторические маршруты/тесты для допрохода.

3. **Выбор шаблона по (коду, версии), конфликтное удаление -> корректная ошибка** — 🟡  
   Базовые template/pipeline проверки проходят в codex-audit, но полный регрессионный прогон остаётся частично красным.

4. **Replace: dry-run -> apply -> rollback** — ⛔  
   API replace в текущем состоянии не закрыт полностью (часть тестов даёт 404/несоответствие ожиданиям).

5. **DocumentJob статусы queue/running/success/error/cancelled** — 🟡  
   Базовые pipeline/job тесты проходят, но полный coverage переходов в одном прогоне не полностью зелёный.

6. **Кабинет клиента: свои статусы/пакеты/файлы/история** — 🟡  
   Frontend сборка/тесты проходят; требуется допроход end-to-end API/ACL для финального sign-off.

7. **Пути дашборд/отчёты/экспорт работают** — 🟡  
   Есть проходящие smoke/feature проверки, но не весь регрессионный срез стабилен.

8. **Risk/PPE/Training/Incidents/Inspections CRUD не ломают поток** — 🟡  
   Большая часть API проверок стабильна; остаются точечные несогласования и политика доступа.

9. **Frontend/Backend не расходятся по ключевым разделам** — 🟡  
   Typecheck/build/tests frontend зелёные, но часть backend API контрактов в полном pytest нуждается в выравнивании.

10. **CI без критических регрессий** — 🟡  
   Критичные исправления внесены, но полный монолитный `pytest -q` и backend lint пока не зелёные.
