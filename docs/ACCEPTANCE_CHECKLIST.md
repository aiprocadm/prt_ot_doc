# ACCEPTANCE CHECKLIST (RC)

Статусы: ✅ выполнено · 🟡 частично · ⛔ заблокировано.

| Критерий | Статус | Что подтверждено в текущем проходе | Что осталось |
|---|---|---|---|
| 1) Повторный POST с тем же `Idempotency-Key` возвращает тот же результат | ✅ | Проверен в replace dry-run с повторным ключом и в существующем idempotency regression-наборе | — |
| 2) Любой бизнес-маршрут без `X-Tenant` -> `400` | ✅ | Обновлены и подтверждены тесты, требующие tenant-header на business API | — |
| 3) Выбор шаблона по `(code, version)`, конфликтное удаление -> корректная ошибка | 🟡 | Базовые template/pipeline тесты проходят в обновленном наборе | Нужен полный шаблонный regression sweep |
| 4) Replace: dry-run -> apply -> rollback | ✅ | Реализован и проходит актуальный API-тест сквозного сценария | — |
| 5) Задания: queued/running/success/error/cancelled | 🟡 | Частично покрыто текущими test-suite | Нужен отдельный job-lifecycle e2e на полном стенде |
| 6) Кабинет клиента: свои статусы/пакеты/файлы/история | 🟡 | Контракты API в этом проходе не ломаются | Требуется UI e2e smoke на наполненных данных |
| 7) Дашборд/отчеты/экспорт работают на базовом уровне | ✅ | Обновленные tests по analytics/search/export проходят | — |
| 8) Risk/PPE/training/incidents не ломают CRUD/чтение | 🟡 | В рамках этого прохода регрессий не выявлено | Нужен полный доменный прогон |
| 9) FE/BE контракты не расходятся по ключевым разделам | ✅ | Исправлены рассогласованные тест-контракты (tenant header, replace API, file keys) | — |
| 10) CI не содержит критических регрессий | 🟡 | Критичный падающий regression-набор стабилизирован | Нужен полный CI цикл (all jobs) |
