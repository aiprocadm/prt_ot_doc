# SEC-63 (разд. 63.3): модульный доступ как поверхность атаки

ТЗ: «Модульный доступ (Доп. №2, разд. 61) сам может стать вектором, если сделан
только на фронте». Ниже — вердикт по каждому из трёх названных рисков.

## Риск 1: обход через прямой API — ЗАКРЫТ

> «Выключенный на фронте модуль доступен по прямому вызову API. → Enforcement на
> backend обязателен».

Все восемь модулей каталога (`FEATURE_CATALOG`) проверяются на бэкенде через
`is_feature_enabled` и отвечают **404** при выключенном модуле:

| Модуль | Где стоит гейт |
|---|---|
| `committees` | `api/routes/committees.py` |
| `contractors` | `api/routes/contractors.py` |
| `medical` | `api/routes/medical/_common.py` |
| `report_builder` | `modules/report_builder/api.py` |
| `budget` | `modules/budget/api.py`, `modules/budget/reimbursement_api.py` |
| `sout` | `api/routes/sout.py` |
| `rules_engine` | `modules/rules_engine/api.py` **и** `engine.py` |
| `warehouse` | `api/routes/ppe.py`, `modules/ppe/stock.py` |

**Чего не хватало:** ничто не мешало добавить девятый модуль и забыть гейт — тогда
«отключённый» модуль остаётся доступным всем, у кого есть токен, и заметить это
можно только пентестом. Ратчет-гард `scripts/ci/check_module_gates.py` (шаг в
`ci.yml`) требует backend-гейт у каждого кода каталога.

## Риск 2: эскалация через зависимости — НЕПРИМЕНИМ СЕЙЧАС

> «Включение модуля-зависимости открывает больше, чем ожидалось. → Проверка
> `depends_on` не должна тихо включать лишнее».

Зависимостей между модулями в каталоге **нет**: `apply_plan` переключает флаги
строго по списку тарифа, ничего не «дотягивая». Риск неприменим — но не навсегда:
гард падает, если в `modules/subscription/plans.py` появится любая константа с
`DEPEND` в имени. Тогда решение об обработке зависимостей придётся принять
осознанно, а не унаследовать молчанием.

## Риск 3: осиротевшие доступы — ЗАКРЫТ ДЛЯ ВЕБХУКОВ (2026-08-03)

> «После отключения модуля остаются активные API-ключи/вебхуки/задачи этого
> модуля. → Отключение модуля деактивирует связанные интеграции».

**Что закрыто.**

* Правила автоматизации (`rules_engine`) проверяют флаг **в момент исполнения**
  (`modules/rules_engine/engine.py`) — после отключения модуля не срабатывают.
* **Вебхуки (2026-08-03):** каталог объявляет события каждого модуля —
  `MODULE_EVENT_TYPES` в `modules/subscription/plans.py` (карта по фактическим
  точкам испускания: contractors ×4, medical ×3, rules_engine ×1; остальные
  модули своих типов не порождают — сквозные `DocumentGenerated`/`PPEIssued`/…
  модулям сознательно НЕ приписаны). Отключение модуля (`apply_plan` с тарифом
  без него, `services/tenants/subscription.py::_prune_module_webhooks`) убирает
  эти типы из `subscribed_events` эндпоинтов арендатора, гасит эндпоинты, у
  которых событий не осталось (пустой список у диспетчера значит «все события» —
  оставить опустевший эндпоинт включённым значило бы расширить доставку), и
  выключает `WebhookSubscription` на события модуля. Автоматического возврата
  при апгрейде нет — платформа не помнит, чьи подписки гасила; вернуть должен
  арендатор. Тесты: `tests/test_module_event_prune.py` (полнота карты против
  каталога и `EventType`, единственность владельца события, прунинг/погашение,
  отсутствие воскрешения).

**Что осталось (отложено осознанно):** API-ключи — у них нет scope по модулям,
деактивировать «ключи модуля» не по чему; закрывается тогда же, когда появится
модульный scope у ключей (та же карта `MODULE_EVENT_TYPES` не поможет — ключи
не событийные). До этого ключ арендатора продолжает работать после понижения
тарифа, но сами module-эндпоинты отвечают 404 (риск 1 закрыт), т.е. доступа к
данным модуля ключ не даёт.

## Раздел 63.1 — пока не на чем

**63.1 Иерархия Reseller → Client**: сама иерархия относится к BIZ-52 (Phase 13,
статус `partial(branding)`). Тестировать «cross-reseller deny» не на чем — как и
раньше, это не отговорка, а отсутствие поверхности. Требования разд. 63.1 —
обязательная часть приёмки BIZ-52, а не отдельная задача «потом».

## Раздел 63.2 — закрыт (2026-08-08)

«Работа в контексте клиента» появилась вместе с BIZ-49, и требования 63.2
закрывались вместе с ней:

| Требование ТЗ | Где сделано |
| --- | --- |
| Имперсонировать может только назначенная роль и только своего клиента | грант в `ManagedClientAccess`, проверка в `dependencies_managed_client.require_client_context`; несуществующий клиент и отсутствие гранта дают ОДИН и тот же отказ |
| Запреты: пароли/2FA, платёжные данные, массовый экспорт ПДн, удаление, настройки безопасности | `domains/managed_clients/impersonation.FORBIDDEN_IN_CONTEXT` + `middleware/impersonation_guard.py` (список объявлен один раз, применяется ко всем роутам) |
| Сессия истекает (60 мин) | `impersonation.CONTEXT_TTL` + `ensure_session_active` |
| Несмываемый аудит «X от имени Y» | метка `on_behalf_of` ставится в `AuditService.log_event` и входит в хэшируемую часть записи |
| Клиент может запросить журнал доступа к своим данным | `GET /api/v1/managed-clients/{mcid}/access-log` |
| Согласие клиента на делегированный доступ | `ManagedClientConsent` (разд. 66.3) |

Два решения, которые стоит помнить:

* **пометка ставится в сервисе аудита, а не на вызывающей стороне.** Точек
  вызова аудита сотни; ручная пометка забылась бы ровно там, где она важнее
  всего, и молча — запись всё равно появится, просто без имени клиента;
* **журнал доступа отдаётся аутсорсеру, а не клиенту напрямую.** У клиента в
  режиме Lightweight учётной записи нет вообще, а в Dedicated следы работы
  аутсорсера физически лежат в ЕГО арендаторе. Показать журнал клиенту —
  обязанность аутсорсера по договору; зеркалирование в арендатора клиента
  упирается в делегированный доступ, то есть в 63.1.

Проверка цепочки аудита при этом оказалась нерабочей и была починена в том же
срезе: подпись считалась по одному значению времени, а в строку писалось
другое, и время из БД сравнивалось сырым (SQLite отдаёт его без часового
пояса). До этого «несмываемость» держалась на честном слове.
