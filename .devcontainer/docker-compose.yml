services:
  devcontainer:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - ..:/workspaces/prt_ot_doc:cached
    working_dir: /workspaces/prt_ot_doc
    env_file:
      - ../.env.example
    command: sleep infinity
