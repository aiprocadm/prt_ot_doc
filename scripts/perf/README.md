# Perf profiles (final stabilization)

Place k6/locust profiles here for stage/perf environment:
- `api_read_profile.js` (key read APIs p95)
- `render_pdf_profile.js` (DOCX->PDF throughput)
- `replace_batch_profile.js` (batch replace throughput)

Current repository run focuses on functional acceptance and reliability checks.
