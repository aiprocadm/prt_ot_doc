from __future__ import annotations

# This module is intentionally empty.
# It was a placeholder and is NOT registered in any router.
# Do not add endpoints here without first defining a clear domain scope and ABAC/RBAC policy.
# Gap: SEC-001 — resolved by explicit documentation of intent.
from fastapi import APIRouter

router = APIRouter()
