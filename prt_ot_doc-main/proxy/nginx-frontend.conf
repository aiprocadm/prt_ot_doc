server {
    listen 80;
    server_name _;

    location / {
        try_files $uri /index.html;
    }

    location = /healthz {
        return 200 '{"status":"ok"}';
        add_header Content-Type application/json;
    }
}
