# Runtime dependencies for API/worker images (see requirements-dev.txt for tests and linters).
aiosqlite==0.20.0
alembic==1.13.3
# asyncpg 0.29 has no wheel for Python 3.13 on Windows (build requires MSVC).
# Keep deterministic pins per Python major/minor to avoid flaky local setup.
asyncpg==0.29.0; python_version < "3.13"
asyncpg==0.31.0; python_version >= "3.13"
boto3==1.35.36
celery==5.4.0
# msgpack: транзитивная (kombu); прямой пин — GHSA-6v7p-g79w-8964 (HIGH,
# out-of-bounds read) закрыт в 1.2.1, docker-сборка иначе ставит 1.1.2.
msgpack==1.2.1
# click: PYSEC-2026-2132 fixed at 8.3.3; click>=8.2 needs typer>=0.16 (see typer pin).
click==8.3.3
docxtpl==0.17.0
email-validator==2.2.0
fastapi==0.141.1
httpx==0.27.2
minio==7.2.10
openpyxl==3.1.5
passlib[argon2]==1.7.4
prometheus-client==0.21.0
pydantic-settings==2.5.2
pydantic==2.9.2
python-docx==1.1.2
# python-dotenv: PYSEC-2026-2270 fixed at 1.2.2.
python-dotenv==1.2.2
python-jose[cryptography]==3.5.0
# cryptography: прямое закрепление поверх транзитивного (тянется python-jose).
# PYSEC-2026-3552 закрыт в 50.0.0. Без своей строки версия оставалась 49.0.0 —
# это БОЕВАЯ библиотека шифрования (подпись токенов входа, шифрование секретов
# арендаторов), поэтому её нельзя оставлять на «что притянулось».
# python-jose требует cryptography>=3.4.0, поэтому 50.x с ней совместима.
cryptography==50.0.1
python-magic==0.4.27
# pyasn1: direct pin to override transitive (pulled by python-jose/cryptography).
# CVE-2026-30922 (HIGH, unbounded recursion DoS) fixed at 0.6.3.
# python-jose 3.5.0 loosened its constraint to pyasn1>=0.5.0 (3.4.0 required <0.5.0),
# so the 0.6.3 pin is resolvable together with python-jose>=3.5.0.
pyasn1>=0.6.3
# python-multipart: PYSEC-2026-3036/3037/3040 fixed at 0.0.31.
python-multipart==0.0.31
redis==5.0.3
slowapi==0.1.9
sqlalchemy==2.0.36
# starlette: direct pin to override transitive (pulled by fastapi).
# 1.3.1 закрывает весь блок PYSEC-2026-161/248/249/1941/1942/2280/2281
# (см. снятые записи pip-audit в .github/security-exceptions.yml).
starlette==1.3.1
greenlet==3.4.0
# typer: bumped alongside click (typer<0.16 breaks on click>=8.2).
typer==0.27.0
uvicorn[standard]==0.30.5
