/**
 * Экраны, которые сегодня не укладываются в UX-бюджет (ТЗ разд. 59.2).
 *
 * ТЗ допускает превышение «только с явным обоснованием и пометкой в ревью» —
 * вот эта пометка. Список существует, чтобы нарушения были ВИДНЫ и посчитаны,
 * а не растворились в «ну, так исторически сложилось».
 *
 * Правила работы со списком:
 *
 * 1. Новое нарушение сюда не добавляют «чтобы позеленело» — сначала чинят
 *    экран. Запись уместна, когда переделка экрана это отдельная работа.
 * 2. Запись обязана точно совпадать с текстом нарушения. Починили экран —
 *    проверка ПАДАЕТ («долг протух») и заставляет убрать запись. Иначе список
 *    однажды станет кладбищем неверных строк, и по нему нельзя будет понять,
 *    сколько экранов на самом деле не в бюджете.
 */
export const UX_BUDGET_DEBT: Record<string, readonly string[]> = {
  // Экран инструктажей: четыре равнозначных действия в шапке и длинная форма
  // назначения. Приведение к «одна задача — один экран» — отдельная работа по
  // разд. 59.1, а не правка на ходу.
  BriefingsPage: [
    "основных действий 3 при лимите 2: оставьте одно, остальные сделайте вторичными",
    "видимых полей 11 при лимите 7: лишнее — под «Дополнительно» или в шаги мастера",
  ],
  // BIZ-60 срез-1, приёмочный прогон: фильтры и форма экрана инцидентов дают
  // восьмое поле. Сжатие фильтров — переделка экрана, отдельная работа.
  IncidentsPage: [
    "видимых полей 8 при лимите 7: лишнее — под «Дополнительно» или в шаги мастера",
  ],
  // BIZ-60 срез-1: фильтры списка вместе с настройками каналов и рассылки
  // дают десять видимых полей. Прогрессивное раскрытие настроек (уровень 2
  // по разд. 59.3) — переделка экрана, отдельная работа.
  NotificationsPage: [
    "видимых полей 10 при лимите 7: лишнее — под «Дополнительно» или в шаги мастера",
  ],
};
