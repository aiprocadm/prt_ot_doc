/**
 * Какие экраны обязаны проходить приёмку UX-бюджета (ТЗ разд. 59.2, 60).
 *
 * Раньше полнота приёмки жила ФРАЗОЙ в отчёте: «81 из 82, вне приёмки только
 * TemplatesPage». Фраза протухла молча — новых экранов с тех пор появилось
 * полтора десятка, и по документу это было не видно. Здесь то же самое
 * записано так, что протухнуть не может: список исключений явный, а сторож
 * сверяет его с реальными файлами страниц.
 *
 * Правила:
 *
 * 1. Новый экран по умолчанию ОБЯЗАН иметь замер бюджета. Не хочешь замер —
 *    впиши сюда причину; молча пропустить нельзя.
 * 2. Причина пишется словами и по существу: «нет времени» причиной не
 *    является, «экран — тонкая обёртка над другим, замер дублировал бы его» —
 *    является.
 * 3. Появился замер у экрана из списка — сторож ПАДАЕТ и требует убрать
 *    запись: иначе список станет кладбищем неверных строк.
 */
export const UX_BUDGET_OUT_OF_SCOPE: Record<string, string> = {
  AccessDeniedPage:
    "Заглушка «нет доступа»: заголовок и ссылка назад, мерить нечего",
  ModuleDisabledPage:
    "Заглушка «модуль не подключён»: сообщение и ссылка, мерить нечего",
  SafetyDashboardPage:
    "Обёртка над DashboardApiPage с другим адресом — замер дублировал бы его",
  TrainingDashboardPage:
    "Обёртка над DashboardApiPage с другим адресом — замер дублировал бы его",
  ClientDeliveryDashboardPage:
    "Обёртка над DashboardApiPage с другим адресом — замер дублировал бы его",
  TemplatesPage:
    "Неизмерим приёмкой: экран целиком строится из данных сервера, в тесте " +
    "рендерится пустым (записано в срезе-1 приёмки)",
};
